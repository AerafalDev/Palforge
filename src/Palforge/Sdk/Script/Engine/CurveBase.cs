// <auto-generated/>
#nullable enable
using Palforge.Unreal.Reflection;
using Palforge.Unreal.Sdk;

namespace Palforge.Sdk.Script.Engine;

public class CurveBase : UObject
{
    internal CurveBase(nint address, UnrealContext context) : base(address, context)
    {
    }

    public static UClass? StaticClass()
    {
        return SdkEnv.StaticClass("CurveBase");
    }

    public static CurveBase? New(UObject? outer = null)
    {
        return SdkEnv.New("CurveBase", outer) as CurveBase;
    }

    public static CurveBase? Load(string path)
    {
        return SdkEnv.LoadAsset(path) as CurveBase;
    }

    public void GetValueRange(out float minValue, out float maxValue)
    {
        var arguments = new byte[][] { Bytes<float>(default), Bytes<float>(default) };
        Call("GetValueRange", arguments, out var outputs);
        minValue = As<float>(outputs[0]);
        maxValue = As<float>(outputs[1]);
    }

    public void GetTimeRange(out float minTime, out float maxTime)
    {
        var arguments = new byte[][] { Bytes<float>(default), Bytes<float>(default) };
        Call("GetTimeRange", arguments, out var outputs);
        minTime = As<float>(outputs[0]);
        maxTime = As<float>(outputs[1]);
    }
}
